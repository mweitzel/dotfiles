# installation and usage

git clone this repo to ~/projects/dotfiles

to create symlinks from ~/projects/dotfiles to ~/.dotfiles and ~/.dotfiles/.* to ~/.*
from within ~/projects/dotfiles directory, run

  $ ./link

to save screenshots in ~/screenshots/
from within ~/projects/dotfiles, run

  $ ./init
